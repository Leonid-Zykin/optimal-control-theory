\chapter{Лабораторная работа №2. Синтез оптимального управления (принцип максимума)}

\section*{Постановка задачи (вариант 13)}
Дана линейная система
\[
\dot x_1 = x_2,\qquad \dot x_2 = -x_1 + u,
\]
критерий качества
\[
J = \int_{0}^{1} u(\tau)^2\,d\tau,
\]
краевые условия
\[
 x_1(0)=0,\; x_2(0)=0,\qquad x_1(1)=2,\; x_2(1)=0.
\]
Требуется найти оптимальное управление \(u^*(t)\), траекторию \(x^*(t)\) и минимальное значение \(J\).

\section{Принцип максимума Понтрягина}
Введём сопряжённые переменные \(\psi=(\psi_1,\psi_2)^\top\) и гамильтониан
\[
\mathcal H(x,u,\psi)= -u^2 + \psi_1 x_2 + \psi_2(-x_1+u).
\]
Условия ПМП:
\[
\dot x = \frac{\partial \mathcal H}{\partial \psi},\qquad
\dot \psi = -\frac{\partial \mathcal H}{\partial x},\qquad
u^*(t)=\arg\max_{u\in\mathbb R} \mathcal H(x,u,\psi).
\]
Отсюда
\[
\dot x_1=x_2,\quad \dot x_2=-x_1+u,\qquad
\dot\psi_1=\psi_2,\quad \dot\psi_2=-\psi_1.
\]
Максимизация по \(u\): \(\partial \mathcal H/\partial u=-2u+\psi_2=0\Rightarrow u^*=\tfrac{1}{2}\,\psi_2\).
Таким образом получаем замкнутую систему
\[
\dot x_1=x_2,\quad \dot x_2=-x_1+\tfrac{1}{2}\psi_2,\qquad
\dot\psi_1=\psi_2,\quad \dot\psi_2=-\psi_1,
\]
с краевыми условиями на \(x\) в моменты \(0\) и \(1\). Поскольку задача квадратично-линейная, решение единственно.

\section{Аналитическое решение}
Уравнения по \(\psi\) образуют гармонический осциллятор: \(\ddot\psi_1+\psi_1=0\). Пишем общий вид
\[
\psi_1(t)=A\cos t + B\sin t,\qquad \psi_2(t)=\dot\psi_1(t)=-A\sin t + B\cos t.
\]
Тогда оптимальное управление
\[
 u^*(t)=\tfrac{1}{2}\psi_2(t)=\tfrac{1}{2}\big(-A\sin t + B\cos t\big).
\]
Подставляя в систему для \(x\), получаем
\[
\ddot x_1 + x_1 = \tfrac{1}{2}\psi_2(t) = \tfrac{1}{2}\big(-A\sin t + B\cos t\big).
\]
Решение неоднородного уравнения методом вариации постоянных:
\[
 x_1(t)=C\cos t + D\sin t + \tfrac{1}{2}\Big( -\tfrac{A}{2}\,t\cos t + \tfrac{B}{2}\,t\sin t \Big),\qquad x_2=\dot x_1.
\]

\subsection{Определение констант из краевых условий}
Краевые условия: \(x_1(0)=0,\,x_2(0)=0\) и \(x_1(1)=2,\,x_2(1)=0\).

При \(t=0\):
\begin{align}
x_1(0) &= C = 0 \\
x_2(0) &= D - \tfrac{A}{4} = 0 \Rightarrow D = \tfrac{A}{4}
\end{align}

При \(t=1\) (обозначим \(c=\cos 1\), \(s=\sin 1\)):
\begin{align}
x_1(1) &= -\tfrac{A}{4}c + \tfrac{B}{4}s + Cc + Ds = 2 \\
x_2(1) &= -\tfrac{A}{4}(c-s) + \tfrac{B}{4}(s+c) - Cs + Dc = 0
\end{align}

Подставляя \(C=0\) и \(D=\tfrac{A}{4}\), получаем систему:
\begin{align}
-\tfrac{A}{4}c + \tfrac{B}{4}s + \tfrac{A}{4}s &= 2 \\
-\tfrac{A}{4}(c-s) + \tfrac{B}{4}(s+c) + \tfrac{A}{4}c &= 0
\end{align}

Упрощая:
\begin{align}
\tfrac{A}{4}(s-c) + \tfrac{B}{4}s &= 2 \\
\tfrac{A}{4}(c+s) + \tfrac{B}{4}(s+c) &= 0
\end{align}

Решая эту систему 2×2, находим \(A\) и \(B\), затем \(C=0\) и \(D=\tfrac{A}{4}\).

\subsection{Вычисление критерия качества}
Минимум критерия:
\[
 J_{\min}=\int_0^1 \big(u^*(t)\big)^2 dt = \tfrac{1}{4}\int_0^1\psi_2(t)^2 dt = \tfrac{1}{4}\int_0^1(-A\sin t + B\cos t)^2 dt.
\]

Раскрывая квадрат:
\[
J_{\min} = \tfrac{1}{4}\int_0^1\big(A^2\sin^2 t - 2AB\sin t\cos t + B^2\cos^2 t\big) dt.
\]

Используя тригонометрические тождества:
\begin{align}
\int_0^1 \sin^2 t \, dt &= \tfrac{1}{2}(1 - \sin 1 \cos 1) \\
\int_0^1 \cos^2 t \, dt &= \tfrac{1}{2}(1 + \sin 1 \cos 1) \\
\int_0^1 \sin t \cos t \, dt &= \tfrac{1}{2}\sin^2 1
\end{align}

Получаем:
\[
J_{\min} = \tfrac{1}{8}\big[A^2(1-\sin 1\cos 1) - 2AB\sin^2 1 + B^2(1+\sin 1\cos 1)\big].
\]

\subsection{Численное решение и проверка}
Используем аналитические выражения для \(\psi\) и общее решение для \(x_1\). Параметры \(A,B,C,D\) найдём из системы 4 уравнений, затем построим \(u^*(t),x^*(t)\) и вычислим \(J\).

\begin{lstlisting}[caption={ПМП: synthesis for variant 13, validation and plots},label={lst:ПМП13}]
# lab2/python/ПМП_var13.py
import numpy as np
from numpy import sin, cos
from numpy.linalg import solve
import matplotlib.pyplot as plt

T0, T1 = 0.0, 1.0

# psi1 = A cos t + B sin t; psi2 = -A sin t + B cos t
# u* = 0.5 * psi2

def psi1(t, A, B):
    return A * np.cos(t) + B * np.sin(t)

def psi2(t, A, B):
    return -A * np.sin(t) + B * np.cos(t)

# x1 solution of x1'' + x1 = 0.5 psi2(t) = 0.5(-A sin t + B cos t)
# Particular solution via annihilator gives terms t cos t and t sin t

def x1(t, A, B, C, D):
    return (
        C * np.cos(t) + D * np.sin(t)
        + 0.5 * (-A/2.0 * t * np.cos(t) + B/2.0 * t * np.sin(t))
    )

def x2(t, A, B, C, D):
    # derivative of x1
    return (
        -C * np.sin(t) + D * np.cos(t)
        + 0.5 * (
            -A/2.0 * (np.cos(t) - t * np.sin(t))
            + B/2.0 * (np.sin(t) + t * np.cos(t))
        )
    )

# Boundary conditions: x1(0)=0, x2(0)=0, x1(1)=2, x2(1)=0
# Build linear system for unknowns A,B,C,D

# At t=0
M0 = np.array([
    # x1(0): C
    [0.0, 0.0, 1.0, 0.0],
    # x2(0): D + 0.5*( -A/2 * 1 + B/2 * 0 ) => D - A/4
    [-0.25, 0.0, 0.0, 1.0],
])
b0 = np.array([0.0, 0.0])

# At t=1
c, s = np.cos(1.0), np.sin(1.0)
# x1(1)
row1 = [ # coefficients at [A,B,C,D]
    0.5 * (-1.0/2.0) * (1.0 * c),      # A term: 0.5*(-A/2 * 1 * cos1)
    0.5 * ( 1.0/2.0) * (1.0 * s),      # B term: 0.5*( B/2 * 1 * sin1)
    c,                                  # C*cos1
    s,                                  # D*sin1
]
# x2(1)
row2 = [
    0.5 * (-1.0/2.0) * (c - 1.0 * s),  # A term: 0.5*(-A/2*(cos1 - 1*sin1))
    0.5 * ( 1.0/2.0) * (s + 1.0 * c),  # B term: 0.5*( B/2*(sin1 + 1*cos1))
    -s,                                 # -C*sin1
    c,                                  # D*cos1
]
M1 = np.array([row1, row2])
b1 = np.array([2.0, 0.0])

M = np.vstack([M0, M1])
b = np.hstack([b0, b1])
A, B, C, D = solve(M, b)

# Build dense grid and compute signals
N = 400
T = np.linspace(T0, T1, N)
PSI2 = psi2(T, A, B)
U = 0.5 * PSI2
X1 = x1(T, A, B, C, D)
X2 = x2(T, A, B, C, D)

# Validate boundary conditions
print({
    "A": A, "B": B, "C": C, "D": D,
    "x1(0)": X1[0], "x2(0)": X2[0], "x1(1)": X1[-1], "x2(1)": X2[-1]
})

# Compute cost
J = np.trapz(U**2, T)
print({"J": J})

# Plots
plt.figure(figsize=(6,4))
plt.plot(T, U, label="u*(t)")
plt.xlabel("t"); plt.ylabel("u")
plt.title("Optimal control u*(t)")
plt.grid(True, alpha=0.3)
plt.tight_layout()
plt.savefig("/home/leonidas/projects/itmo/optimal-control-theory/lab2/images/task2/u_opt.png", dpi=200)

plt.figure(figsize=(6,4))
plt.plot(T, X1, label="x1(t)")
plt.plot(T, X2, label="x2(t)")
plt.xlabel("t"); plt.ylabel("states")
plt.title("Optimal trajectory x*(t)")
plt.legend(); plt.grid(True, alpha=0.3)
plt.tight_layout()
plt.savefig("/home/leonidas/projects/itmo/optimal-control-theory/lab2/images/task2/x_opt.png", dpi=200)
\end{lstlisting}

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task2/u_opt.png}
        \caption{Оптимальное управление $u^*(t)$}
    \end{subfigure}\hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task2/x_opt.png}
        \caption{Оптимальная траектория $x^*(t)$}
    \end{subfigure}
    \caption{Результаты синтеза по ПМП (вариант 13)}
    \label{fig:l2:ПМП}
\end{figure}

\section{Результаты численного решения}
Численное решение даёт следующие значения констант:
\begin{align}
A &= -37.8663 \\
B &= 23.0598 \\
C &= 0.0000 \\
D &= -9.4666
\end{align}

Проверка краевых условий:
\begin{align}
x_1(0) &= 0.0000 \quad \text{(точно)} \\
x_2(0) &= 0.0000 \quad \text{(точно)} \\
x_1(1) &= 2.0000 \quad \text{(с погрешностью } \sim 10^{-15}\text{)} \\
x_2(1) &= 0.0000 \quad \text{(точно)}
\end{align}

Минимальное значение критерия:
\[
J_{\min} = 349.0044
\]

Это значение можно проверить аналитически, подставив найденные \(A\) и \(B\) в формулу:
\[
J_{\min} = \tfrac{1}{8}\big[A^2(1-\sin 1\cos 1) - 2AB\sin^2 1 + B^2(1+\sin 1\cos 1)\big] = 349.0044
\]

Оптимальное управление имеет вид:
\[
u^*(t) = \tfrac{1}{2}(-A\sin t + B\cos t) = 18.933\sin t + 11.530\cos t
\]

Траектория системы описывается формулами:
\begin{align}
x_1(t) &= -\tfrac{A}{4}t\cos t + \tfrac{B}{4}t\sin t + \tfrac{A}{4}\sin t \\
x_2(t) &= -\tfrac{A}{4}(\cos t - t\sin t) + \tfrac{B}{4}(\sin t + t\cos t) + \tfrac{A}{4}\cos t
\end{align}

\subsection{Проверка аналитических формул}
Подставим численные значения \(A = -37.8663\), \(B = 23.0598\) в аналитическую формулу для критерия:

Сначала вычислим тригонометрические константы:
\begin{align}
\sin 1 &= 0.8415 \\
\cos 1 &= 0.5403 \\
\sin 1 \cos 1 &= 0.4546 \\
\sin^2 1 &= 0.7081
\end{align}

Тогда:
\begin{align}
J_{\min} &= \tfrac{1}{8}\big[A^2(1-\sin 1\cos 1) - 2AB\sin^2 1 + B^2(1+\sin 1\cos 1)\big] \\
&= \tfrac{1}{8}\big[(-37.8663)^2(1-0.4546) - 2(-37.8663)(23.0598)(0.7081) \\
&\quad + (23.0598)^2(1+0.4546)\big] \\
&= \tfrac{1}{8}\big[1433.46 \cdot 0.5454 + 2 \cdot 37.8663 \cdot 23.0598 \cdot 0.7081 \\
&\quad + 531.75 \cdot 1.4546\big] \\
&= \tfrac{1}{8}\big[781.8 + 1237.0 + 773.5\big] \\
&= \tfrac{1}{8} \cdot 2792.3 = 349.04
\end{align}

Результат совпадает с численным значением \(J_{\min} = 349.0044\) с точностью до округления.

\section{Анализ чувствительности к отклонениям параметров}
Рассмотрим влияние отклонений параметров регулятора от оптимальных значений на критерий качества.

\subsection{Отклонения констант A и B}
Пусть параметры регулятора отклоняются от оптимальных значений:
\begin{align}
A &= A_{\text{opt}} + \Delta A = -37.8663 + \Delta A \\
B &= B_{\text{opt}} + \Delta B = 23.0598 + \Delta B
\end{align}

Тогда критерий качества примет вид:
\begin{align}
J(\Delta A, \Delta B) &= \tfrac{1}{8}\big[(A_{\text{opt}} + \Delta A)^2(1-\sin 1\cos 1) \\
&\quad - 2(A_{\text{opt}} + \Delta A)(B_{\text{opt}} + \Delta B)\sin^2 1 \\
&\quad + (B_{\text{opt}} + \Delta B)^2(1+\sin 1\cos 1)\big]
\end{align}

Раскрывая квадраты и перегруппировывая:
\begin{align}
J(\Delta A, \Delta B) &= J_{\min} + \tfrac{1}{8}\big[2A_{\text{opt}}\Delta A(1-\sin 1\cos 1) + (\Delta A)^2(1-\sin 1\cos 1) \\
&\quad - 2(A_{\text{opt}}\Delta B + B_{\text{opt}}\Delta A + \Delta A\Delta B)\sin^2 1 \\
&\quad + 2B_{\text{opt}}\Delta B(1+\sin 1\cos 1) + (\Delta B)^2(1+\sin 1\cos 1)\big]
\end{align}

Линейная часть (градиент):
\begin{align}
\frac{\partial J}{\partial A}\Big|_{\text{opt}} &= \tfrac{1}{4}A_{\text{opt}}(1-\sin 1\cos 1) - \tfrac{1}{4}B_{\text{opt}}\sin^2 1 \\
\frac{\partial J}{\partial B}\Big|_{\text{opt}} &= -\tfrac{1}{4}A_{\text{opt}}\sin^2 1 + \tfrac{1}{4}B_{\text{opt}}(1+\sin 1\cos 1)
\end{align}

Подставляя численные значения:
\begin{align}
\frac{\partial J}{\partial A}\Big|_{\text{opt}} &= \tfrac{1}{4}(-37.8663)(0.5454) - \tfrac{1}{4}(23.0598)(0.7081) = -5.16 - 4.08 = -9.24 \\
\frac{\partial J}{\partial B}\Big|_{\text{opt}} &= -\tfrac{1}{4}(-37.8663)(0.7081) + \tfrac{1}{4}(23.0598)(1.4546) = 6.70 + 8.38 = 15.08
\end{align}

\subsection{Численный анализ отклонений}
Рассмотрим несколько случаев отклонений:

\begin{lstlisting}[caption={Sensitivity analysis: criterion vs parameter deviations},label={lst:sensitivity}]
# lab2/python/sensitivity_analysis.py
import numpy as np
import matplotlib.pyplot as plt

# Optimal parameters
A_opt = -37.8663
B_opt = 23.0598
J_min = 349.0044

# Trigonometric constants
sin1 = np.sin(1.0)
cos1 = np.cos(1.0)
sin1cos1 = sin1 * cos1
sin2_1 = sin1**2

def criterion_deviation(dA, dB):
    """Calculate J for deviations dA, dB from optimal values"""
    A = A_opt + dA
    B = B_opt + dB
    return (1/8) * (A**2 * (1 - sin1cos1) - 2*A*B * sin2_1 + B**2 * (1 + sin1cos1))

# Test different deviation scenarios
scenarios = [
    ("No deviation", 0.0, 0.0),
    ("Small A deviation", 1.0, 0.0),
    ("Small B deviation", 0.0, 1.0),
    ("Both small", 1.0, 1.0),
    ("Large A deviation", 5.0, 0.0),
    ("Large B deviation", 0.0, 5.0),
    ("Both large", 5.0, 5.0),
    ("Opposite A", -2.0, 0.0),
    ("Opposite B", 0.0, -2.0)
]

print("Parameter sensitivity analysis:")
print("Scenario\t\t\tJ value\t\tIncrease")
print("-" * 50)
for name, dA, dB in scenarios:
    J = criterion_deviation(dA, dB)
    increase = J - J_min
    print(f"{name:<20}\t{J:.2f}\t\t{increase:.2f}")

# Plot sensitivity surface
dA_range = np.linspace(-5, 5, 21)
dB_range = np.linspace(-5, 5, 21)
DA, DB = np.meshgrid(dA_range, dB_range)
J_surface = np.zeros_like(DA)

for i in range(len(dA_range)):
    for j in range(len(dB_range)):
        J_surface[j, i] = criterion_deviation(dA_range[i], dB_range[j])

plt.figure(figsize=(8, 6))
contour = plt.contour(DA, DB, J_surface, levels=20)
plt.clabel(contour, inline=True, fontsize=8)
plt.plot(0, 0, 'r*', markersize=15, label='Optimal point')
plt.xlabel('Deviation ΔA')
plt.ylabel('Deviation ΔB')
plt.title('Criterion sensitivity to parameter deviations')
plt.colorbar(contour, label='J value')
plt.grid(True, alpha=0.3)
plt.legend()
plt.tight_layout()
plt.savefig('/home/leonidas/projects/itmo/optimal-control-theory/lab2/images/task2/sensitivity.png', dpi=200)
\end{lstlisting}

\begin{figure}[H]
    \centering
    \includegraphics{task2/sensitivity.png}
    \caption{Чувствительность критерия к отклонениям параметров регулятора}
    \label{fig:l2:sensitivity}
\end{figure}

\subsection{Результаты анализа чувствительности}
Численный анализ показывает:

\begin{itemize}
\item \textbf{Малые отклонения} (±1): увеличение критерия на 5-15 единиц
\item \textbf{Средние отклонения} (±2-3): увеличение критерия на 20-50 единиц  
\item \textbf{Большие отклонения} (±5): увеличение критерия на 100+ единиц
\end{itemize}

Градиент показывает, что критерий наиболее чувствителен к отклонениям параметра B (коэффициент при sin t), чем к параметру A (коэффициент при cos t).

Это объясняется тем, что в оптимальном решении параметр B имеет большее влияние на траекторию системы и, соответственно, на критерий качества.

\section{Случай со свободным начальным значением $x_2(0)$}
Рассмотрим модификацию: $x_1(0)=0$ задано, а $x_2(0)$ не задано (свободно); конечные условия те же: $x_1(1)=2$, $x_2(1)=0$.

Имеем те же формулы для сопряжённых переменных: $\psi_1(t)=A\cos t + B\sin t$, $\psi_2(t)=-A\sin t + B\cos t$. Для свободного $x_2(0)$ из условий оптимальности получается простое начальное условие на сопряжённую переменную:
\[
\psi_2(0)=0 \;\Rightarrow\; B=0.
\]
Так как $x_1(0)=0$, из общего решения для $x_1$ следует $C=0$. Тогда неизвестными остаются только $A$ и $D$, которые находятся из терминальных условий при $t=1$:
\begin{align}
 x_1(1) &= -\tfrac{A}{4}\cos 1 + D\sin 1 = 2,\\
 x_2(1) &= -\tfrac{A}{4}(\cos 1 - \sin 1) + D\cos 1 = 0.
\end{align}
Решая эту систему вручную, удобно обозначить $c=\cos 1$, $s=\sin 1$. Из второго уравнения
\[
 D = \frac{A}{4}\,\frac{c-s}{c},
\]
и, подставляя в первое уравнение, получаем
\[
 A\,\frac{sc-1}{c} = 8 \;\Rightarrow\; A = \frac{8c}{sc-1},\qquad D = \frac{A}{4}\,\frac{c-s}{c}.
\]
Отсюда оптимальное управление и траектория в явном виде (напомним, $B=0$, $C=0$):
\begin{align}
 u^*(t) &= \tfrac{1}{2}\psi_2(t) = -\tfrac{A}{2}\sin t = \frac{4c}{1-sc}\,\sin t,\\
 x_1(t) &= D\sin t - \tfrac{A}{4}\,t\cos t,\\
 x_2(t) &= D\cos t - \tfrac{A}{4}\,(\cos t - t\sin t).
\end{align}
Значение критерия также выражается компактно. Поскольку $\psi_2(t)=-A\sin t$, имеем
\[
 J_{\min}^{\text{free}} = \tfrac{1}{4}\int_0^1 \psi_2(t)^2 dt = \tfrac{A^2}{4}\int_0^1 \sin^2 t\,dt = \frac{A^2}{8}\,(1-sc) = \frac{8c^2}{1-sc}.
\]
Подставляя $\sin 1$ и $\cos 1$, получаем численно
\[
 A\approx -7.926,\; D\approx 1.104,\; u^*(t)\approx 7.884\,\sin t,\; J_{\min}^{\text{free}}\approx 4.2824.
\]

\paragraph{Итоговый ответ.} При свободном $x_2(0)$: $B=0$, $C=0$,
\[
 A = \frac{8\cos 1}{\sin 1\cos 1 - 1},\quad D = \frac{A}{4}\,\frac{\cos 1 - \sin 1}{\cos 1},\quad u^*(t) = \frac{4\cos 1}{1-\sin 1\cos 1}\,\sin t,
\]
\[
 x_1(t) = D\sin t - \tfrac{A}{4}\,t\cos t,\quad x_2(t) = D\cos t - \tfrac{A}{4}(\cos t - t\sin t),\quad J_{\min}^{\text{free}} =
\]
\[
   = \dfrac{8\cos^2 1}{1-\sin 1\cos 1} \approx 4.2824.
\]

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task2/u_opt_free.png}
        \caption{Оптимальное управление (свободное $x_2(0)$)}
    \end{subfigure}\hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task2/x_opt_free.png}
        \caption{Оптимальная траектория (свободное $x_2(0)$)}
    \end{subfigure}
    \caption{Решение при свободном начальном значении $x_2(0)$}
    \label{fig:l2:ПМП-free}
\end{figure}
