\chapter{Лабораторная работа №5. Оптимальный наблюдатель (фильтр Калмана) и ЛКГ-синтез}

\section*{Постановка (вариант 13)}
Рассматривается линейный объект в присутствии аддитивных «белых» шумов процесса и измерений:
\[
\dot x = Ax + bu + Gw,\qquad y=Cx+v,
\]
где
\[
A=\begin{bmatrix}0&1\\-4&-7\end{bmatrix},\quad
b=\begin{bmatrix}3\\2\end{bmatrix},\quad
C=\begin{bmatrix}1&0\end{bmatrix},\quad
G=I_2,
\]
а ковариации шумов задаются постоянными спектральными плотностями
\[
W=\begin{bmatrix}7&4.5\\4.5&6\end{bmatrix},\qquad V=2.
\]
Задача — построить оптимальный наблюдатель Калмана–Бьюси вида
\(\dot{\hat x}=A\hat x+bu+L(y-C\hat x)\), минимизирующий математическое ожидание \(J=\mathrm{E}\{e_h^\top e_h\}\), где \(e_h=x-\hat x\). Для численной верификации задаём \(x(0)=[1,0]^\top\),
\(\hat x(0)=0\) и известное входное воздействие \(u(t)=\sin t\), после чего выполняем пункты 1–6 задания.

\section{Уравнение Риккати наблюдателя и матрица усилений}
Ковариационная матрица ошибки фильтра удовлетворяет уравнению Риккати наблюдателя (dual CARE):
\[
A P + P A^\top + G W G^\top - P C^\top V^{-1} C P = 0,\qquad L = P C^\top V^{-1}.
\]
Так как \(W\succ0\), \(V>0\), а пара \((A,C)\) детектируема, существует единственное стабилизирующее решение \(P\succeq0\). Численное решение \(\texttt{solve\_continuous\_are}\) даёт
\[
P=\begin{bmatrix}1.962 & 0.410\\ 0.410 & 0.303\end{bmatrix},\qquad
L=P C^\top V^{-1}=\begin{bmatrix}1.6197\\ -0.4383\end{bmatrix}.
\]
Полученные коэффициенты обеспечивают полюса матрицы \(A-LC\) в \(-2.08\pm2.54j\), то есть наблюдатель устойчив и достаточно быстрый.

Для дальнейшего анализа написан единый сценарий \(\texttt{python/kalman\_var13.py}\). Он:
\begin{enumerate}
    \item вычисляет усиление \(L\) по заданным \(W,V\);
    \item моделирует ошибки \(e_h(t)\) и интегральный критерий \(J(0,t)=\int_0^t \|e_h(\tau)\|^2 d\tau\);
    \item автоматически пересчитывает задачи 3–5 при модификации \(L\), \(W\) или \(V\);
    \item формирует графики и итоговую таблицу показателей (см. раздел~\ref{sec:summary-table});
    \item дополняет модель регулятором ЛКГ в пункте~6.
\end{enumerate}

\section{Номинальное моделирование (пункты 1–2)}
На рис.~\ref{fig:nominal} показаны временные зависимости ошибок и интегрального критерия при \(u(t)=\sin t\) в присутствии шумов процесса \(w(t)\) и измерений \(v(t)\). Ошибка наблюдателя демонстрирует стохастическое поведение с накопленным критерием \(J(0,10)=31.60\), что отражает влияние случайных возмущений на качество оценивания.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/error_nominal.png}
        \caption{Ошибки \(e_{h1},e_{h2}\)}
    \end{subfigure}\hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/u_input.png}
        \caption{Задающее воздействие \(u=\sin t\)}
    \end{subfigure}

    \vspace{0.5em}

    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/J_nominal.png}
        \caption{Интеграл J(0,t)}
    \end{subfigure}
    \caption{Номинальный наблюдатель Калмана (пункты 1–2)}
    \label{fig:nominal}
\end{figure}

\section{Чувствительность к отклонению матрицы L (пункт 3)}
Для проверки устойчивости зададим небольшое асимметричное изменение коэффициентов наблюдателя:
\[
L_\text{pert}=\begin{bmatrix}1.15 & 0 \\ 0 & 0.85\end{bmatrix}L.
\]
Вход и начальные условия оставлены прежними, используются те же реализации шумов. Графики нормы ошибки и интеграла \(J(0,t)\) (рис.~\ref{fig:L-pert}) показывают, что наблюдатель сохраняет устойчивость, а итоговый критерий немного снижается (\(30.71\) против \(31.60\)) за счёт более агрессивного оценивающего контура. Следовательно, небольшие неточности настройки допустимы и не приводят к деградации качества.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/error_norm_L.png}
        \caption{Норма ошибки вектора \(e_h\)}
    \end{subfigure}\hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/J_compare_L.png}
        \caption{Сравнение интеграла J(0,t)}
    \end{subfigure}
    \caption{Последствия изменения коэффициентов \(L\) на \(\pm15\%\)}
    \label{fig:L-pert}
\end{figure}

\section{Изменение спектральной плотности процесса W (пункт 4)}
Матрицу шума процесса увеличим и сделаем менее коррелированной,
\(
W_\text{mod}=\begin{bmatrix}8.5&3.8\\3.8&5.5\end{bmatrix}\succ0.
\)
Решение уравнения Риккати даёт более крупные значения \(P\) и, соответственно, усиление \(L\), компенсирующее рост неопределённости. Ошибка возрастает в переходном процессе из-за усиленного шума процесса, но остаётся ограниченной (рис.~\ref{fig:W}). Критерий \(J(0,10)=34.32\) увеличивается по сравнению с номиналом (\(31.60\)) из-за более интенсивного шума процесса.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/error_W.png}
        \caption{Ошибки при изменённом $W$}
    \end{subfigure}\hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/J_W.png}
        \caption{Интеграл J(0,t) при изменённом $W$}
    \end{subfigure}

    \vspace{0.5em}

    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/error_norm_W.png}
        \caption{Сравнение нормы ошибки}
    \end{subfigure}\hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/J_compare_W.png}
        \caption{Сравнение интеграла J(0,t)}
    \end{subfigure}
    \caption{Влияние изменения ковариации процесса}
    \label{fig:W}
\end{figure}

\section{Изменение спектральной плотности измерений V (пункт 5)}
Повышение уровня измерительного шума до \(V_\text{mod}=3\) приводит к более осторожному наблюдателю: усиление \(L\) уменьшается, фильтр меньше доверяет измерениям, больше полагается на модель. Это увеличивает критерий до \(J(0,10)=36.57\) (рис.~\ref{fig:V}) по сравнению с номиналом (\(31.60\)) из-за более интенсивного шума измерений. Тем не менее система остаётся устойчивой.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/error_V.png}
        \caption{Ошибки при \(V_\text{mod}\)}
    \end{subfigure}\hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/J_V.png}
        \caption{Интеграл J(0,t) при увеличенном $V$}
    \end{subfigure}

    \vspace{0.5em}

    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/error_norm_V.png}
        \caption{Сравнение нормы ошибки}
    \end{subfigure}\hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/J_compare_V.png}
        \caption{Сравнение интеграла J(0,t)}
    \end{subfigure}
    \caption{Влияние изменения ковариации измерений}
    \label{fig:V}
\end{figure}

\section{ЛКГ-синтез и полностью замкнутая система (пункт 6)}
На завершающем этапе соединяем наблюдатель с регулятором, строя линейно-квадратичный гауссовский (ЛКГ) контур. Регулятор выбирается как оптимальный LQR:
\[
u=-K\hat x,\qquad
K=r^{-1}b^\top P_r,\quad
P_r=A^\top P_r + P_r A - P_r b r^{-1} b^\top P_r + Q = 0.
\]
Взяты веса из задания~2 (\(Q=\mathrm{diag}(6,3)\), \(r=2\)), что даёт \(K=[1.1345\;\;1.8229]\). Моделирование (рис.~\ref{fig:lqg}) показывает, что реальные состояния и оценки совпадают с высокой точностью, закон управления стабилизирует объект без внешнего сигнала \(u(t)\), а критерий для наблюдателя \(J(0,10)=31.60\) совпадает с номинальным случаем, так как используются те же шумы.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/states_LQG.png}
        \caption{Состояния и их оценки}
    \end{subfigure}\hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/u_LQG.png}
        \caption{Управление \(u=-K\hat x\)}
    \end{subfigure}

    \vspace{0.5em}

    \begin{subfigure}{0.48\textwidth}
        \includegraphics{task5/error_LQG.png}
        \caption{Норма ошибки наблюдателя}
    \end{subfigure}
    \caption{Полностью замкнутая ЛКГ-система}
    \label{fig:lqg}
\end{figure}

\section{Сводные показатели качества}
\label{sec:summary-table}
Для всех сценариев вычислены конечный критерий и максимальная норма ошибки:
\[
J(0,10)=\int_0^{10}\|e_h(\tau)\|^2d\tau.
\]
Из-за наличия шумов ошибка не входит в узкую полосу \(|e_h|<0.02\), поэтому время входа в полосу не вычисляется.

\begin{table}[H]
    \centering
    \begin{tabular}{lcc}
        \toprule
        Сценарий & \(J(0,10)\) & \(\max_t\|e_h\|\) \\
        \midrule
        Номинальный \(W,V\) & 31.60 & 6.00 \\
        \(L\) с \(\pm15\%\) & 30.71 & 5.78 \\
        Модифицированный \(W\) & 34.32 & 6.35 \\
        Увеличенный \(V\) & 36.57 & 6.52 \\
        ЛКГ (наблюдатель + LQR) & 31.60 & 6.00 \\
        \bottomrule
    \end{tabular}
    \caption{Сравнение показателей всех пунктов задания}
\end{table}

Таблица подчёркивает ожидаемые тенденции: повышение измерительного шума сильнее ухудшает критерий (\(36.57\) против \(31.60\)), чем изменение \(W\) (\(34.32\)) или \(L\) (\(30.71\)); подключение регулятора сохраняет качество наблюдателя, одновременно подавляя собственные колебания объекта. Таким образом, отчёт охватывает все пункты 1–6, включая моделирование с учётом шумов, сравнительный анализ и ЛКГ-синтез.

